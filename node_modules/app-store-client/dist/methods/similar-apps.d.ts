import { AppStoreClientOptions } from "../client";
import { App } from "../types";
export interface SimilarOptions extends AppStoreClientOptions {
    id?: string;
    appId?: string;
}
export default function similarApps(options: SimilarOptions): Promise<App[]>;
