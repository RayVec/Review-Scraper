import { Review } from "../types";
import { AppStoreClientOptions } from "../client";
export interface ReviewOptions extends AppStoreClientOptions {
    id?: string;
    appId?: string;
    sort?: string;
    page?: number;
}
export default function reviews(options: ReviewOptions): Promise<Review[]>;
