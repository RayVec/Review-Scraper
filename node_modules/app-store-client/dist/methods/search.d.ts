import { App } from "../types";
import { AppStoreClientOptions } from "../client";
export interface SearchOptions extends AppStoreClientOptions {
    term: string;
    num?: number;
    page?: number;
    idsOnly?: boolean;
}
export default function search(options: SearchOptions): Promise<App[] | string[]>;
