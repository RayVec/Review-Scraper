import { App } from "../types";
import { AppStoreClientOptions } from "../client";
export interface AppOptions extends AppStoreClientOptions {
    id?: string;
    appId?: string;
    ratings?: boolean;
}
export default function app(options: AppOptions): Promise<App>;
