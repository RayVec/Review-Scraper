import { Collection, Category } from "../constants";
import { App } from "../types";
import { AppStoreClientOptions } from "../client";
export interface ListOptions extends AppStoreClientOptions {
    collection?: Collection;
    category?: Category;
    num?: number;
    fullDetail?: boolean;
}
export default function list(options: ListOptions): Promise<App[]>;
